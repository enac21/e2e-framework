// Package ports defines the interfaces (contracts) that the core domain
// relies on. Adapters implement these interfaces to connect the domain
// to external infrastructure. This file defines the Assertion interface,
// which validates a field of a NormalizedMessage against an expected value.
package ports
