// Package services contains the core business logic for the e2e-testing-service.
// This file defines the Orchestrator, which coordinates the full test lifecycle.
// The orchestrator only depends on port interfaces — it never imports from adapters.
package services
